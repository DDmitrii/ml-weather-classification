project_name: ml_weather
seed: 42

data:
  batch_size: 64

training:
  epochs: 15
  learning_rate: 0.0003
  optimizer: adam
  use_scheduler: true

mlflow:
  enabled: true
  experiment_name: weather_classification
  run_name: resnet18_exp_lr3e4_bs64
  tags:
    project: ml_weather
    stage: experiment
